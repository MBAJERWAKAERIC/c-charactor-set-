#include <stdio.h>
#include <stdlib.h>

#include<stdio.h>
#include<conio.h>
int main()
{
	int count = 0;
	while(count <= 255)
    {
         printf("ASCII value of %c is %d\n", count, count);
        count++;

    }


	return 0;
}
